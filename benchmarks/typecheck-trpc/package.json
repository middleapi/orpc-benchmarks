{
  "name": "typecheck-trpc",
  "version": "0.0.0",
  "license": "MIT",
  "type": "module",
  "scripts": {
    "bench": "tsx scripts/gen.ts && tsc --noEmit --incremental false --extendedDiagnostics"
  }
}
